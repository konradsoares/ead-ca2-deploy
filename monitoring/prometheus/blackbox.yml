modules:
  http_2xx:
    prober: http
    timeout: 5s
    http:
      method: GET
      valid_http_versions: ["HTTP/1.1", "HTTP/2"]
      valid_status_codes: [200]
      fail_if_ssl: false
      fail_if_body_missing: true
      fail_if_body_not_matches_regexp:
        - "OK"
